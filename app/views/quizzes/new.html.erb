<% content_for(:title) { "Create Quiz - FIQ" } %>

<div class="max-w-4xl mx-auto">
  <h1 class="text-4xl font-black text-white mb-8 flex items-center gap-3">
    <span class="text-5xl">✨</span> Create New Quiz
  </h1>
  
  <%= render "form", quiz: @quiz %>
</div>
